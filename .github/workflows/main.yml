name: MANUAL BUILD
on:
  workflow_dispatch
jobs:
  build:
    runs-on: [self-hosted, NEWUNIFIED]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: echo "harish"  
